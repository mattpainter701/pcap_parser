# Benchmark Example

```bash
pcap-parser capture.pcapng --benchmark

# Custom benchmark output path
pcap-parser capture.pcapng --benchmark --benchmark-output results/perf.json
```

Output includes wall time, CPU time, peak memory, throughput, and top hot functions.
